/**
 * Barrel export for all custom hooks.
 * Import hooks from here for a clean, stable import path.
 *
 * @example
 *   import { useAppDispatch, useAppSelector, usePetDecay } from '../hooks';
 */

export * from './useRedux';
export { usePetDecay } from './usePetDecay';
