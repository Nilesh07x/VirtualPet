/**
 * Navigation root.
 *
 * AppNavigator manages the four-screen game flow using a custom
 * Animated crossfade + slide transition — no external navigation
 * library required.
 *
 * Screens: Home → Feed / Clean / Sleep
 *
 * @example
 *   import { AppNavigator } from '../navigation';
 */

export { AppNavigator } from './AppNavigator';
export type { ScreenName } from './AppNavigator';
